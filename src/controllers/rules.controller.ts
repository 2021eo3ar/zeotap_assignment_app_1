import { Request, Response } from "express";

export const combieRuleController = async (req: Request, res: Response) => {
  // need to make that
};
export const evaluateRuleController = async (req: Request, res: Response) => {
  // need to make that
};
export const createRuleController = async (req: Request, res: Response) => {
  // need to make that
};
