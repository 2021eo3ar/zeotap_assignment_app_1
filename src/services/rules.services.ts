import postgresdb from "../config/db.config"
import { rules } from "../models/schema"


export const createRuleService = async()=>{
//need to make that completely 
}
export const combieRuleService = async()=>{
//need to make that completely 
}
export const evaluateRuleService = async()=>{
//need to make that completely 
}
